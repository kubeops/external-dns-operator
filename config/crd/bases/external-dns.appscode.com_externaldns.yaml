---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.2
  creationTimestamp: null
  name: externaldns.external-dns.appscode.com
spec:
  group: external-dns.appscode.com
  names:
    kind: ExternalDNS
    listKind: ExternalDNSList
    plural: externaldns
    singular: externaldns
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ExternalDNS is the Schema for the externaldns API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ExternalDNSSpec defines the desired state of ExternalDNS
            properties:
              apiServerURL:
                description: The kubernetes API server to connect to
                type: string
              entries:
                description: Information about entries
                items:
                  properties:
                    policy:
                      description: 'Modify how DNS records are synchronized between
                        sources and providers (default: sync, options: sync, upsert-only,
                        create-only)'
                      type: string
                    provider:
                      description: Provider information
                      properties:
                        aws:
                          description: AWS provider information
                          properties:
                            awsAPIRetries:
                              description: When using the AWS provider, set the maximum
                                number of retries for API calls before giving up.
                              type: integer
                            awsAssumeRole:
                              description: When using the AWS provider, assume this
                                IAM role. Useful for hosted zones in another AWS account.
                                Specify the full ARN, e.g. `arn:aws:iam::123455567:role/external-dns`
                              type: string
                            awsBatchChangeInterval:
                              description: When using the AWS provider, set the interval
                                between batch changes.
                              format: int64
                              type: integer
                            awsBatchChangeSize:
                              description: When using AWS provide, set the maximum
                                number of changes that will be applied in each batch
                              type: integer
                            awsEvaluateTargetHealth:
                              description: 'When using the AWS provider, set whether
                                to evaluate the health of the DNS target (default:
                                enable, disable with --no-aws-evaluate-target-health)'
                              type: boolean
                            awsPreferCNAME:
                              description: 'When using the AWS provider, prefer using
                                CNAME instead of ALIAS (default: disabled)'
                              type: boolean
                            awsSDServiceCleanup:
                              description: 'When using the AWS CloudMap provider,
                                delete empty Services without endpoints (default:
                                disabled)'
                              type: boolean
                            awsZoneCacheDuration:
                              description: When using the AWS provider, set the zones
                                list cache TTL (0s to disable).
                              format: int64
                              type: integer
                            awsZoneTagFilter:
                              description: When using the AWS provider, filter for
                                zones with these tags
                              items:
                                type: string
                              type: array
                            awsZoneType:
                              description: 'When using the AWS provider, filter for
                                zones of this type. (support: public, private)'
                              type: string
                          type: object
                        cloudflare:
                          description: Cloudflare provider information
                          properties:
                            cloudflareProxied:
                              description: 'When using the Cloudflare provider, specify
                                if the proxy mode must be enabled (default: disabled)'
                              type: boolean
                            cloudflareZonesPerPage:
                              description: 'When using the Cloudflare provider, specify
                                how many zones per page listed, max. possible 50 (default:
                                50)'
                              type: integer
                          type: object
                        domainFilter:
                          description: Limit possible target zones by a domain suffix
                          items:
                            type: string
                          type: array
                        excludeDomains:
                          description: Exclude subdomains
                          items:
                            type: string
                          type: array
                        name:
                          description: The DNS provider where the DNS records will
                            be created. (AWS, Cloudflare)
                          type: string
                        zoneIDFilter:
                          description: Filter target zones by hosted zone id
                          items:
                            type: string
                          type: array
                      type: object
                    registry:
                      description: Registry information
                      properties:
                        txtOwnerID:
                          description: 'When using the TXT registry, a name that identifies
                            this instance of ExternalDNS (default: default)'
                          type: string
                        txtPrefix:
                          description: When using the TXT registry, a custom string
                            that's prefixed to each ownership DNS record (optional).
                            Could contain record type template like '%{record_type}-prefix-'.
                            Mutual exclusive with txt-suffix!
                          type: string
                        txtSuffix:
                          description: When using the TXT registry, a custom string
                            that's suffixed to the host portion of each ownership
                            DNS record. Could contain record type template like '-%{record_type}-suffix'.
                            Mutual exclusive with txt-prefix!
                          type: string
                        txtWildcardReplacement:
                          description: When using the TXT registry, a custom string
                            that's used instead of an asterisk for TXT records corresponding
                            to wildcard DNS records
                          type: string
                        type:
                          description: 'The registry implementation to use to keep
                            track of DNS record ownership (default: txt, options:
                            txt, noop, aws-sd)'
                          type: string
                      type: object
                    sources:
                      description: Sources information
                      properties:
                        alwaysPublishNotReadyAddresses:
                          description: Always publish also not ready addresses for
                            headless services
                          type: boolean
                        annotationFilter:
                          description: Filter sources managed by external-dns via
                            label selector when listing all resources
                          type: string
                        combineFQDNAndAnnotation:
                          description: Combine FQDN template and Annotations instead
                            of overwriting
                          type: boolean
                        compatibility:
                          description: Process  annotation semantics from legacy implementations
                          type: string
                        connectorSourceServer:
                          description: The server to connect for connector source,
                            valid only when using connector source
                          type: string
                        defaultTargets:
                          description: Set globally a list of default IP address that
                            will apply as a target instead of source addresses.
                          items:
                            type: string
                          type: array
                        fqdnTemplate:
                          description: A templated string that's used to generate
                            DNS names from source that don't define a hostname themselves,
                            or to add a hostname suffix when paired with the fake
                            source
                          type: string
                        gatewayLabelFilter:
                          description: Filter Gateways of Route endpoints via label
                            selector
                          type: string
                        gatewayNamespace:
                          description: Limit Gateways of route endpoints to a specific
                            namespace
                          type: string
                        ignoreHostnameAnnotation:
                          description: Ignore hostname annotation when generating
                            DNS names, valid only when using fqdn-template is set
                          type: boolean
                        ignoreIngressRulesSpec:
                          description: Ignore rules spec section in ingresses resources,
                            applicable only for ingress sources
                          type: boolean
                        ignoreIngressTLSSpec:
                          description: Ignore TLS Spec section in ingresses resources,
                            applicable only for ingress source
                          type: boolean
                        labelFilter:
                          description: Filter sources managed by external-dns via
                            annotation using label selector semantics
                          type: string
                        manageDNSRecordTypes:
                          description: 'Comma separated list of record types to manage
                            (default: A, CNAME; supported: A,CNAME,NS)'
                          items:
                            type: string
                          type: array
                        names:
                          description: 'The resource types that are queried for endpoints;
                            List of source. ex: source, ingress, node etc.'
                          items:
                            type: string
                          type: array
                        namespace:
                          description: 'Limit sources of endpoints to a specific namespace
                            (default: all namespaces)'
                          type: string
                        ocRouterName:
                          description: If source is openshift router then you can
                            pass the ingress controller name. Based on this name the
                            external dns will select the respective router from the
                            route status and map that routeCanonicalHostname to the
                            route host while creating a CNAME record.
                          type: string
                        publishHostIP:
                          description: Allow external-dns to publish host-ip for headless
                            services
                          type: boolean
                        publishInternal:
                          description: Allow  externals-dns to publish DNS records
                            for ClusterIP services
                          type: boolean
                        serviceTypeFilter:
                          description: 'The service types to take care about (default
                            all, expected: ClusterIP, NodePort, LoadBalancer or ExternalName)'
                          items:
                            type: string
                          type: array
                      type: object
                  type: object
                type: array
              kubeconfig:
                description: Path to kubernetes configuration file
                type: string
              requestTimeout:
                description: Request timeout when calling Kubernetes API. 0s means
                  no timeout
                format: int64
                type: integer
            type: object
          status:
            description: ExternalDNSStatus defines the observed state of ExternalDNS
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
