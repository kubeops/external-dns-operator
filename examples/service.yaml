apiVersion: v1
kind: Service
metadata:
  name: myapp
  namespace: demo
  annotations:
    external-dns.alpha.kubernetes.io/hostname: dmo-service.prod-test.link
spec:
  selector:
    app: myapp
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
